import styles from "./header.module.css"
const Header = () => {
    return (
        <header className={styles.headerContent}>
          <h1>header</h1>  
        </header>
    );
};

export default Header;