export type Productos = {
    quantity: number,
    price: number,
    available: boolean,
    sublevel_id: number,
    name: string,
    id: string,
}[];
export type Producto = {
    quantity: number,
    price: number,
    available: boolean,
    sublevel_id: number,
    name: string,
    id: string,
};